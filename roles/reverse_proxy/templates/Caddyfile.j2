# {{ ansible_managed }}
{% if tailscale_domain is defined %}
(tailscale) {
	tls {
		get_certificate tailscale
	}
}

http://{{ tailscale_domain }} {
	redir https://{{ tailscale_domain }}{uri}
}

import conf.d/*

{% endif %}
