---
- name: Reload systemd for postgres
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
  changed_when: true
- name: Restart postgres
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  become: true
  loop: "{{ enabled_services }}"
