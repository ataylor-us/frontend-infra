---
- name: Reload systemd for homeassistant
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
  changed_when: true
- name: Restart homeassistant
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  become: true
  loop: "{{ enabled_services }}"
