---
- name: Reload systemd for miniflux
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
  changed_when: true
- name: Restart miniflux
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  become: true
  loop: "{{ enabled_services }}"
