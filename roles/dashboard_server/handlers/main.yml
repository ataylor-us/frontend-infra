---
- name: Reload systemd for homer
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
  changed_when: true
- name: Restart homer
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: restarted
  become: true
  loop: "{{ enabled_services }}"
