---
title: "{{ ansible_hostname }}"
footer: false

services:
  - name: "Files"
    icon: "fas fa-folder-open"
    items:
      - name: "Syncthing"
        subtitle: "File sync"
        url: "https://{{ tailscale_domain }}:{{ syncthing_external_port }}"
        target: "_blank"
